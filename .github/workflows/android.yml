name: Android CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Verify Java & Gradle
        run: |
          java -version
          ./gradlew --version

      # Build while skipping all lint tasks (lint, lintDebug, lintRelease, lintVitalRelease)
      - name: Build with Gradle (skip all lint)
        run: ./gradlew build -x lint -x lintDebug -x lintRelease -x lintVitalRelease --stacktrace --warning-mode all
